#login(ng-show="visibleLogin")
	.title Member Login
	input.username(ng-model="username" type="text" placeholder="USERNAME")
	input.password(ng-model="password" type="text" placeholder="PASSWORD")
	.attemptLogin(ng-click="login()") Login
	.security
		strong Warning!
		| Passwords can't be reset or recovered! For your security, your password is never sent to our servers. We have no way to recover your account if you lose your password. Please keep it safe!
